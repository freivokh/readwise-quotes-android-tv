<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/settings_background">

    <!-- Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="32dp"
        android:paddingTop="32dp"
        android:paddingBottom="24dp">

        <Button
            android:id="@+id/backButton"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:text="‹"
            android:textSize="24sp"
            android:textColor="@color/settings_text_primary"
            android:background="@drawable/settings_focusable_item"
            android:focusable="true"
            android:nextFocusDown="@id/railAccount"
            android:nextFocusRight="@id/railAccount" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/settings_title"
            android:textColor="@color/settings_text_primary"
            android:textSize="24sp"
            android:fontFamily="sans-serif-light"
            android:layout_marginStart="16dp" />

    </LinearLayout>

    <!-- Main content: Rail + Content area -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal"
        android:paddingHorizontal="32dp"
        android:paddingBottom="32dp">

        <!-- Left Rail -->
        <LinearLayout
            android:id="@+id/settingsRail"
            android:layout_width="200dp"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:background="@drawable/settings_card_background"
            android:paddingVertical="16dp">

            <TextView
                android:id="@+id/railAccount"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="  👤  Account"
                android:textSize="14sp"
                android:textColor="@drawable/rail_item_text_color"
                android:background="@drawable/rail_item_background"
                android:paddingVertical="16dp"
                android:paddingHorizontal="20dp"
                android:layout_marginHorizontal="8dp"
                android:layout_marginBottom="4dp"
                android:focusable="true"
                android:nextFocusUp="@id/backButton"
                android:nextFocusDown="@id/railFilters" />

            <TextView
                android:id="@+id/railFilters"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="  🏷️  Filters"
                android:textSize="14sp"
                android:textColor="@drawable/rail_item_text_color"
                android:background="@drawable/rail_item_background"
                android:paddingVertical="16dp"
                android:paddingHorizontal="20dp"
                android:layout_marginHorizontal="8dp"
                android:layout_marginBottom="4dp"
                android:focusable="true"
                android:nextFocusUp="@id/railAccount"
                android:nextFocusDown="@id/railDisplay" />

            <TextView
                android:id="@+id/railDisplay"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="  🎨  Display"
                android:textSize="14sp"
                android:textColor="@drawable/rail_item_text_color"
                android:background="@drawable/rail_item_background"
                android:paddingVertical="16dp"
                android:paddingHorizontal="20dp"
                android:layout_marginHorizontal="8dp"
                android:layout_marginBottom="4dp"
                android:focusable="true"
                android:nextFocusUp="@id/railFilters"
                android:nextFocusDown="@id/railSync" />

            <TextView
                android:id="@+id/railSync"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="  🔄  Sync"
                android:textSize="14sp"
                android:textColor="@drawable/rail_item_text_color"
                android:background="@drawable/rail_item_background"
                android:paddingVertical="16dp"
                android:paddingHorizontal="20dp"
                android:layout_marginHorizontal="8dp"
                android:focusable="true"
                android:nextFocusUp="@id/railDisplay" />

        </LinearLayout>

        <!-- Content Area -->
        <FrameLayout
            android:id="@+id/contentArea"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:layout_marginStart="24dp" />

    </LinearLayout>

</LinearLayout>
